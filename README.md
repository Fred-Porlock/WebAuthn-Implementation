## 前端
安装依赖
```powershell
npm install
```

运行前端
```powershell
npm run dev
```

## 后端
创建并激活虚拟环境
```powershell
python -m venv .venv
.\.venv\Scripts\activate
```

安装webauthn和flask库
```powershell
pip install flask webauthn
```

运行后端app.py
```powershell
flask run
```